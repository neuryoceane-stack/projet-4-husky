# Copie ce fichier en .env.local et remplis avec les VRAIES valeurs
# (elles sont sur Vercel : projet → Settings → Environment Variables)
# Ne partage jamais .env.local ni ne le mets sur GitHub.

POSTGRES_URL=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Optionnel : pour le formulaire de réservation (emails)
RESEND_API_KEY=
